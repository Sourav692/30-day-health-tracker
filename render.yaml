services:
  - type: web
    name: 30-day-health-tracker
    runtime: node
    buildCommand: echo "No build needed"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
